# Bachelor thesis TeX project

In order to create a clean PDF, it is recommended to run `pdflatex` twice.
I personally use 

```sh
pdflatex thesis && pdflatex thesis && open thesis.pdf
```

to create the PDF and preview it.
